import { Canvas, Meta, Source } from '@storybook/blocks'

import { ApiTable } from '@/components'

import * as UseMountStories from './useMount.stories.ts'
import { apiParamsMeta } from './apiMeta.ts'

<Meta of={UseMountStories} />

# useMount

仅在组件 mount 时执行一次回调

## 代码演示

### 基础用法

<Canvas of={UseMountStories.Default} />

```tsx
import { Box, Button } from '@mui/material'
import { FC } from 'react'

import { useBoolean, useMount } from 'plasticine-hooks'

const Foo: FC = () => {
  useMount(() => {
    window.alert('mount')
  })

  return <Box>Foo</Box>
}

const Demo: FC = () => {
  const [mounted, { toggle }] = useBoolean(false)

  return (
    <Box>
      <Button variant="contained" onClick={toggle}>
        {mounted ? 'unmount' : 'mount'}
      </Button>

      {mounted && <Foo />}
    </Box>
  )
}

export default Demo
```

## API

### 类型定义

```ts
useMount(callback: () => void): void
```

### 参数

<ApiTable rows={apiParamsMeta} />
